# 学習履歴ページ 追加ガイド

## 仕組みの概要

```
public/learning/
  aws-saa.md             ← フロントマター付きのmdを追加するだけでカードが増える
  aws-sap.md
  azure-xxx.md           ← 新しい資格はここに追加するだけ
```

**カード追加の流れ（これだけ）**

1. `public/learning/{id}.md` を追加する（フロントマターを先頭に書く）
2. ビルドまたはdev server保存時に `manifest.json` が自動生成される
3. ホームページのカードが自動で増える

`manifest.json` は自動生成されるので手動編集不要。

---

## mdファイルの先頭に書くフロントマター

```yaml
---
provider: AWS
exam: Solutions Architect Associate
examCode: SAA-C03
description: カードに表示する1〜2行の説明文
status: completed
---
```

| フィールド | 説明 |
|-----------|------|
| provider | フィルタータブに使われる。`AWS` / `Azure` / `GCP` など |
| exam | カードに表示される試験名 |
| examCode | カードヘッダーに小さく表示される試験コード |
| description | カードに表示する1〜2行の説明文 |
| status | `completed` または `in-progress`（学習中バッジが出る） |

**例：Azure AZ-900 を追加する場合（ファイル名: `azure-az900.md`）**

```yaml
---
provider: Azure
exam: Azure Fundamentals
examCode: AZ-900
description: Azureの基礎概念・コアサービス・セキュリティ・価格モデルをハンズオンで実践
status: completed
---

# Azure AZ-900 ハンズオン学習記録
...（本文）
```

---

## md ファイルの書き方（テイスト統一ルール）

### 基本構成（必ずこの順番で）

```markdown
# {資格名} ハンズオン学習記録

## 概要

{資格の位置づけ}の取得に向け、{領域列挙}を体系的にハンズオンで実践しました。
「{学習の軸となる問い}」という判断まで含めた{能力}を養う学習を積み上げています。

| 領域 | 身についた設計力 |
|------|----------------|
| 領域名 | 〜設計・〜構成・〜実装 |

---

## 領域別 詳細

### 1. {領域名}

{何を実践したか、どんな環境/構成を作ったか。2〜3段落}

**設計判断の実践例**
- {AサービスとBサービスの使い分け}
- {なぜこの設計を選んだかの判断基準}
- {トレードオフを理解した上での選択}

---

（以下、領域ごとに繰り返す）

---

*{資格名}は、{試験の位置づけ・レベル説明}。{本文のまとめ}。*
```

### テイスト統一ルール（必ず守る）

| ルール | OK例 | NG例 |
|--------|------|------|
| 「できる設計力」で書く | 「VPC Peering vs TGWの使い分けを実践」 | 「VPC PeeringとTGWを使った」 |
| 具体的な判断基準を入れる | 「接続数・スケーラビリティで選定」 | 「接続した」 |
| 技術名の羅列にしない | 「Multi-AZ構成でフェイルオーバーを検証」 | 「RDS / Aurora / DynamoDB / ElastiCache」 |
| ラボ数・時間の具体数を書かない | （書かない） | 「21ラボ実施」「50時間学習」 |
| 最後に資格の位置づけを1行で締める | 「最上位のProfessional認定」 | （省略しない） |

---

## AIへの指示方法

### ChatGPT / Claude に渡すプロンプト

```
以下の学習記録ファイルを読んで、ポートフォリオサイト用の学習履歴ページを作成してください。

【制約】
- 技術名・サービス名の羅列にしない
- 「何を学んだか」ではなく「どんな設計判断ができるようになったか」で書く
- ラボ数や時間の具体的な数値は一切書かない
- 各領域に「設計判断の実践例」として3〜4点の箇条書きを入れる
- 採用担当者が3〜5分で読める分量にする

【出力フォーマット】
このファイルに記載されているMarkdownテンプレートに従って出力すること。
（LEARNING_GUIDE.md の「md ファイルの書き方」セクションを参照）

【資格情報】
- 資格名: {例: AWS Developer Associate}
- 試験コード: {例: DVA-C02}
- 位置づけ: {例: AWSのアプリケーション開発者向けAssociate認定}

【学習記録】
（ここに small.md の内容を貼り付ける）
```

### 指示の手順

1. このファイル（LEARNING_GUIDE.md）の内容をAIに読み込ませる
2. 上記プロンプトを使い、`small.md` の内容を貼り付けて指示する
3. 出力されたmdを `public/learning/{id}.md` に保存する
4. `manifest.json` に1エントリ追記する
5. 完成

---

## ファイル命名規則

| 資格 | id（ファイル名） | provider |
|------|----------------|----------|
| AWS系 | `aws-{コード小文字}` 例: `aws-dva` | `AWS` |
| Azure系 | `azure-{コード小文字}` 例: `azure-az900` | `Azure` |
| GCP系 | `gcp-{コード小文字}` 例: `gcp-ace` | `GCP` |
| その他 | `{プロバイダ}-{識別子}` | 任意 |

---

## 既存ファイルの参照

テイストの参考として、既存ファイルを見てください。

- `public/learning/aws-saa.md` — SAA（Associate）レベルの書き方例
- `public/learning/aws-sap.md` — SAP（Professional）レベルの書き方例
